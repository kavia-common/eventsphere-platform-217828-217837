# EventSphere Frontend - .env.example

# Frontend absolute URL
REACT_APP_FRONTEND_URL=http://localhost:3000

# Backend base URLs (Apollo appends /graphql)
# HTTP endpoint for queries/mutations
REACT_APP_BACKEND_URL=http://localhost:4000
# WebSocket endpoint for subscriptions (use ws:// in dev, wss:// in prod)
REACT_APP_WS_URL=ws://localhost:4000

# Optional convenience alias (not required)
REACT_APP_API_BASE=http://localhost:4000

# Env/Build toggles
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_ENABLE_SOURCE_MAPS=true
REACT_APP_PORT=3000
REACT_APP_TRUST_PROXY=false
REACT_APP_LOG_LEVEL=info
REACT_APP_HEALTHCHECK_PATH=/healthz

# Feature flags
REACT_APP_FEATURE_FLAGS=chat,analytics
REACT_APP_EXPERIMENTS_ENABLED=false
